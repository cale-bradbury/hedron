{
  "name": "@hedron-gl/dmx-lighting",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs --out-dir dist",
    "dev": "tsup src/index.ts --watch --dts --format esm,cjs --out-dir dist"
  },
  "dependencies": {
    "@hedron-gl/engine": "*"
  },
  "devDependencies": {
    "tsup": "^8.3.6",
    "typescript": "^5.6.2"
  }
}
